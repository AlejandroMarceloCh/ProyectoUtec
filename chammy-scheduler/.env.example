# Coordenadas por defecto para el pronóstico del clima (Open-Meteo API).
# El usuario puede sobrescribir esto desde el modal de "Configuración" en la UI.
# Estos valores sólo se usan la primera vez que se abre la app (antes de guardar settings).

VITE_DEFAULT_LAT=39.1972
VITE_DEFAULT_LON=-120.2354
VITE_DEFAULT_LOCATION_NAME="Palisades Tahoe · Olympic Valley, CA"
VITE_RESTAURANT_NAME="Chammy"
